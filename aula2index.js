const prompt = require("prompt-sync");
let number1 = ("Digite seu peso em kg: ");
let number2 = ("Digite sua altura em cm: ");
const Math = (number1 / number2 * number2)
console.log("Seu IMC é de ", Math);